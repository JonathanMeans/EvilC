find_package (Python COMPONENTS Interpreter)
if (NOT ${Python_FOUND})
    message(FATAL_ERROR "Python Interpreter not found.")
endif()
add_test(NAME E2ETests COMMAND ${Python_EXECUTABLE} ${CMAKE_CURRENT_LIST_DIR}/run_e2e_tests.py $<TARGET_FILE:EvilC>)